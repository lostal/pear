# Local development
DATABASE_URL=sqlite:///./pear.db
# Production (con Docker, la DB se persiste en el volumen data/db):
# DATABASE_URL=sqlite:///./data/pear.db

JWT_SECRET=cambia-esta-clave-en-produccion
JWT_EXPIRE_MINUTES=60

# Opcional — tiene valores por defecto en core/config.py
# JWT_ALGORITHM=HS256
# UPLOADS_DIR=uploads
# SITEMAP_DOMAIN=https://pearstore.duckdns.org
