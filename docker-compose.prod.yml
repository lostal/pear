services:
  backend:
    build: ./backend
    container_name: backend
    restart: always
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    working_dir: /app
    command: sh -c "uv run uvicorn app.main:app --host 0.0.0.0 --port 3001"

volumes:
  backend_data:
